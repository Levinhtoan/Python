version: '3.8'

services:
  booking-api:
    build: .
    container_name: booking_management_api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
